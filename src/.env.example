# Nuxt Configuration
NUXT_APP_BASE_URL=/

# Authentication (for backend/admin endpoints)
AUTH_ENABLED=false
AUTH_TYPE=token
AUTH_TOKEN=your-secret-token-here

# OR use Basic Auth
# AUTH_TYPE=basic
# AUTH_USERNAME=admin
# AUTH_PASSWORD=secure-password
