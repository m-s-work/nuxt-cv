import { describe, it, expect } from 'vitest'

describe('Basic Test', () => {
  it('should pass', () => {
    expect(true).toBe(true)
  })

  it('can do basic math', () => {
    expect(1 + 1).toBe(2)
  })
})

